## 0.1.0

- Initial release
- Type-safe primitives: `AccountId`, `CryptoHash`, `NearToken`, `PublicKey`
- Block reference types with sealed class pattern
- JSON-RPC request/response types
- `RpcResult` sealed class for exhaustive pattern matching
- Full test coverage
